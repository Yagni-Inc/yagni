//This function will eventually validate that an email address is connected to a customer in our database.

const emailValidator = email => {
	console.log(email);
	return true;
};

export default emailValidator;
